# jumpUp_miniHackathon
점프업 캠프 미니 해커톤

## ai 사용
> 제미나이 : 코드 기초 틀 \
> 클로드 : 에러 및 디자인 수정

## 최초 1회

```
git pull origin main --allow-unrelated-histories 
git remote remove origin
git remote add origin 저장소주소
```

## 커밋 및 푸시

```
git add .
git commit -m "커밋 메시지" 
git branch -m master main #(1회만 사용)
git push -u origin main
```
